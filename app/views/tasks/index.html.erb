<h1>index page</h1>
<%= link_to "Create new task", new_task_path, :class => "btn btn-primary m-1" %>
<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.name, task_path(task) %></li>
  <% end %>
</ul>
